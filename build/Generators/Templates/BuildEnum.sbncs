{{- func build_enum(enum_data) }}
{{ enum_data.docs }}
public enum {{ enum_data.name }}
{
    {{- for entry in enum_data.entries }}
    {{ entry.docs }}
    {{ entry.name }},
    {{- end }}
}
{{ end -}}