link:
  - file: {{ string.capitalize spec.name }}.tp.yml
  {{- for library in libraries }}
  - dif: {{ library.spec.name }}
  {{- end -}}
copy:
  - { file: {{ string.capitalize spec.name }}{{ suffix.us }}.build.cs, procContent: true }